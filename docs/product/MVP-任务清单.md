# SuitAgent Workspace MVP 任务清单

## 已完成

### 1. 产品文档
- [x] 输出 `docs/product/PRD-半自动案件工作台.md`
- [x] 输出 `docs/product/开发计划-半自动案件工作台.md`
- [x] 明确首版产品形态为 Web 案件工作台 + 外部执行适配层

### 2. 后端 MVP
- [x] 搭建 `apps/api` FastAPI 服务
- [x] 实现案件、文档、任务、产物核心接口
- [x] 支持案件创建与案件详情查询
- [x] 支持文档上传与文本提取
- [x] 支持任务创建、失败重试和时间线记录
- [x] 支持产物审核、审核历史和导出
- [x] 将本地 JSON 持久化升级为 SQLite 数据库存储
- [x] 引入 `WorkflowRun` 与 `ReviewRecord`
- [x] 新增文档详情预览接口
- [x] 新增产物导出接口

### 3. 前端 MVP
- [x] 搭建 `apps/web` Next.js 前端
- [x] 实现案件列表页和新建案件入口
- [x] 实现案件工作台页
- [x] 接入后端 API
- [x] 支持文档上传、提取预览和分类展示
- [x] 支持任务发起、任务日志查看和失败重试
- [x] 支持产物查看、审核、审核历史和导出
- [x] 支持工作流运行记录展示

### 4. 验证
- [x] `npm run test:api`
- [x] `npm --prefix apps/web run lint`
- [x] `npm run build:web`

## 当前结论

这份 MVP 任务清单已经完成。当前仓库已经具备一个可运行、可演示、可继续扩展的内部工作台原型。

## 下一阶段任务

### P1. 执行层升级
- [x] 定义可替换的 Claude Code 类执行适配协议
- [x] 将本地执行器降级为 fallback
- [x] 增加任务状态同步与取消接口
- [x] 接入可执行的外部命令执行器适配层
- [ ] 增加任务轮询与结果回传的真实通信链路
- [ ] 在可用认证环境下打通真实 `claude -p` 执行

### P2. 团队可用性
- [ ] 增加基础登录与角色权限
- [ ] 增加模板配置与管理页面
- [ ] 增加更多文档处理链路接入
- [ ] 增加导出链路的格式与错误处理

### P3. 工程化
- [ ] 引入 Postgres 替代 SQLite
- [ ] 引入 Redis/队列系统
- [ ] 增加更完整的端到端与回归测试
